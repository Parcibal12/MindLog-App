name: mindlog_app
description: "A new Flutter project."
publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: ^3.11.3

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  flutter_riverpod: ^2.5.1
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  dio: ^5.4.3
  retrofit: ^4.9.2
  flutter_svg: ^2.0.10
  json_annotation: ^4.8.1
  mindlog_design_system:
    git:
      url: https://github.com/Parcibal12/mindlog_design_system.git
      ref: main
  http: ^1.6.0
  local_auth: ^3.0.1
  go_router: ^17.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  build_runner: ^2.4.8
  retrofit_generator: ^10.2.6
  json_serializable: ^6.8.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/